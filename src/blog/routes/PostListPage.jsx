// Placeholder — category/search/sort filtering lands in slice 14.
const PostListPage = () => {
  return <div className="py-24 text-center text-gray-500">Post list — coming in a later slice.</div>;
};

export default PostListPage;
