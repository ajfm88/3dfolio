// Route root for the blog. Placeholder for now — the light-theme shell, providers,
// and nested routes land in later slices (see blog-plan.md).
const Blog = () => {
  return (
    <div className="min-h-screen bg-white text-gray-900 flex flex-col items-center justify-center gap-3">
      <h1 className="text-3xl font-bold">Blog</h1>
      <p className="text-gray-500">Coming soon.</p>
    </div>
  );
};

export default Blog;
